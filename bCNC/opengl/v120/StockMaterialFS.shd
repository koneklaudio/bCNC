#version 120

uniform vec3 light1dir;
uniform vec3 light2dir;

varying vec3 normal_;
varying float height;

void main() {
    float R = (height + 20) / 20;
    gl_FragColor = vec4(vec3(R, .5, .5) * abs(dot(normal_, light1dir) + dot(normal_, light2dir) / 2.), 1.0);
}